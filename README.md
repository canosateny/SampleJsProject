# SampleJsProject

## Running Locally

To run this static JavaScript site locally:

```bash
npx serve .
```

This will start a local development server. Open your browser and navigate to the provided URL (typically `http://localhost:3000`).